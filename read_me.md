open the git bash 
check the present working directory (pwd)
check the directory , if directory is not present create directory using mkdir command 
add the directory using cd directory_name
then start to clone 
git clone urs (url you will get from repo code)
then type ls you will get the directory present in github
To create shellscript use she-bang
open the file in file_name.sh
first line should be she-bang #!/bin/bash
enter the code
press esc save the file using :wq! code
./file_name.sh
give the input to get the output
use git status command 
then git add . command to add the files
use the git commit -m "content which is present in the file"
git checkout -b name of the branch
git status
git add .
use git commit 
use git merge command to marge 
then push command to push the file from local to remote repo
